Catalog
    *Windows:	..\totalcmd_IT\

Info:
    * %COMMANDER_PATH%\Utilites\ — Софт в сборке
    * %COMMANDER_PATH%\wincmd.ini — все настройки непосредственно файлового менеджера
    * %COMMANDER_PATH%\wcx_ftp.ini — настройки встроенного FTP-клиента, в т.ч. сохраненные пароли
    * %COMMANDER_PATH%\usercmd.ini — пользовательские команды
    %COMMANDER_PATH% — директория, откуда запущен Total Commander.